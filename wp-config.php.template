<?php
/**
 * Generated by the wordpress-frankenphp image on first run.
 * Edit via the container environment, or remove this file from the
 * volume and restart the container to regenerate it.
 */

$env = static function ( string $key, string $default = '' ): string {
	$value = getenv( $key );
	return false === $value ? $default : $value;
};

// Handle TLS terminated by a reverse proxy (EasyPanel / Caddy / Traefik).
if ( isset( $_SERVER['HTTP_X_FORWARDED_PROTO'] ) && 'https' === $_SERVER['HTTP_X_FORWARDED_PROTO'] ) {
	$_SERVER['HTTPS'] = 'on';
}

// ** Database settings.
define( 'DB_NAME', '__DB_NAME__' );
define( 'DB_USER', '__DB_USER__' );
define( 'DB_PASSWORD', '__DB_PASSWORD__' );
define( 'DB_HOST', '__DB_HOST__' );
define( 'DB_CHARSET', '__DB_CHARSET__' );
define( 'DB_COLLATE', '__DB_COLLATE__' );

// ** Authentication unique keys and salts.
__AUTH_KEYS__

/**
 * WordPress database table prefix.
 */
$table_prefix = '__TABLE_PREFIX__';

define( 'WP_DEBUG', false );

// Allow plugin/theme installs and updates without FTP credentials.
define( 'FS_METHOD', 'direct' );

// Optional fixed URLs (uncomment by setting the WP_HOME / WP_SITEURL env vars).
if ( $env( 'WP_HOME' ) !== '' ) {
	define( 'WP_HOME', $env( 'WP_HOME' ) );
}
if ( $env( 'WP_SITEURL' ) !== '' ) {
	define( 'WP_SITEURL', $env( 'WP_SITEURL' ) );
}

/* Add any custom values between this line and the "stop editing" line. */

/* That's all, stop editing! Happy publishing. */

/** Absolute path to the WordPress directory. */
if ( ! defined( 'ABSPATH' ) ) {
	define( 'ABSPATH', __DIR__ . '/' );
}

/** Sets up WordPress vars and included files. */
require_once ABSPATH . 'wp-settings.php';
